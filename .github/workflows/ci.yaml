name: testingdockeraction

on:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: checkoutcode
        uses: actions/checkout@v3

      - name: Run action
        uses: ./
        id: hello-docker-action
        with:
          usertext: ayoub

        
      - name: output text
        run: echo "${{ steps.hello-docker-action.outputs.greeting }}"