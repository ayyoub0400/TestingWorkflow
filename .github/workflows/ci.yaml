name: helloworkflow
on: [push]
jobs:
  job:
    runs-on: ubuntu-latest
    steps:
      - name: text
        env:
          filename: test
          filetext: test2
        run: |
          echo "$filetext" > $filename.txt
          cat $filename.txt
