name: myworkflow
on: [push]
jobs: 
  job:
    runs-on: ubuntu-latest
    steps:
      - name: checkoutcode
        uses: actions/checkout@v4
        
      - name: helloworld
        run: cat hello.txt  