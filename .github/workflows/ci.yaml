name: helloworkflow
on: push

jobs:
  job:
    strategy:
      matrix:
        os: [ubuntu-latest,windows-latest]
    runs-on: ${{matrix.os}}
    steps:
      - name: dockerhelloworld 
        run: docker run helloworld

      
    
